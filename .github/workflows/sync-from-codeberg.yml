name: Sync
on: workflow_dispatch

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - run: |
        git pull https://codeberg.org/yumou/huh114514.github.io.git main --allow-unrelated-histories
        git push